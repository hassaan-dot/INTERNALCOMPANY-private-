import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { },
    header: { fontSize: 16, fontWeight: "700", textAlign: "center", marginBottom: 20, color: "#CCD9FF" },
   
  });